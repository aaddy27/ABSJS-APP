import 'package:flutter/material.dart';

class DownloadsTab extends StatelessWidget {
  const DownloadsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("⬇️ डाउनलोड स्क्रीन", style: TextStyle(fontSize: 16)),
    );
  }
}
